# Design

## Visual Theme

**"من الفريزر للزيت"** — سلسلة تبريد صناعية تقابل لحظة قلي سينمائية.

الصفحة **مغموسة (drenched)** في الكحلي العميق بتاع العلبة. الكحلي هو الفريزر: هادي، ساكن، بلا ضوضاء. البرتقالي هو الحرارة: ما بيظهرش كزينة أبدًا — بس مكان ما فيه فعل (أزرار، أرقام حرارة، لحظة القلي). التلج (frost) لون تالت للحالة الباردة.

القرار ده مقصود ضد أربع لاندينجات متوقّعة: مش خلفية كريمي/بيج (الديفولت المشبع)، مش أبيض SaaS، مش مجلة طعام بخط مزخرف، مش أحمر/أصفر دليفري.

الموقع بيشتغل بـ **نورين**: الكحلي المغموس، ونهار بارد للناس اللي بتفتح الصفحة على موبايل في شمس الظهر أو بتشتغل على اللوحة طول اليوم تحت نور مكتب. مش ثيم تاني — نفس التصميم، مصدر ضوء تاني (شوف [نورين، تصميم واحد](#نورين-تصميم-واحد)).

**Register:** brand · **استراتيجية اللون:** Drenched (سطح واحد مشبع + إشارة برتقالية محسوبة)

## Color Palette

كله OKLCH، وكله في [`resources/css/theme.css`](resources/css/theme.css) — ملف واحد بيخدم الموقع واللوحة.

### نورين، تصميم واحد

الموقع اتبنى **مغموس في الكحلي** (الفريزر). الوضع الفاتح مش عكس الألوان، ده **نفس الغرفة في نهار**: نفس التركيب، نفس الإيقاع، نفس الأدوار — بس مصدر الضوء اتغيّر.

كل لون متعرّف مرّتين كـ **دور (role)**، وبالليت الـ Tailwind بيتربط على الأدوار دي عن طريق `@theme inline`:

```css
@theme inline { --color-abyss: var(--ground); }   /* bg-abyss → var(--ground) */
```

`inline` دي حمّالة: من غيرها الـ utility بيتجمّد على قيمة `:root` وقت البناء. بيها القيمة بتتحلّ **عند العنصر نفسه**، يعني أي جزء من الصفحة يقدر يحطّ `data-theme` ويولّع نفسه لوحده. ستيدج المنتج في الهيرو معتمد على ده بالظبط (تحت).

### قراءة الأسماء القديمة

أسماء Tailwind هي مفردات الخامة بتاعة البراند وما اتغيرتش: **`abyss` دايمًا أعمق سطح، و`cream` دايمًا الحبر**. في النهار أعمق سطح بيبقى أبيض بارد والحبر بيبقى كحلي — الاسم بيوعد بالموقع على السلّم، مش بالهيكس.

**وفي التلات حالات اللي الوعد ده بيقع فيها، فيه توكن حقيقي:**

| Token | ليه موجود |
|---|---|
| `--on-flame` | الحبر اللي **فوق** الفيل البرتقالي. غامق في **الوضعين**، لأن الفيل برتقالي في الوضعين. `text-abyss` على زرار flame كان هيبقى أبيض على برتقالي (2.3:1). |
| `--flame-ink` / `--flame-ink-hi` | برتقالي **تقدر تقراه**. فيل البرتقالي 3.2:1 على أرضية فاتحة: كفاية لحدّ زرار، ممنوع لليبل 12px. |
| `--frost-ink` | نفس القسمة للأزرق البارد. |
| `--field-line` | حدّ حقل الإدخال. ده **أفوردانس مش زخرفة** — WCAG 1.4.11 عايز 3:1، والهيرلاين الرمادي ما كانش بيوصلها في أي وضع. |
| `--invert` | باند المواصفات، اللي دايمًا عكس الصفحة: ورق في الضلمة، كحلي في النهار. |

### الأرضيات

| الدور | Dark | Light | الاستخدام |
|---|---|---|---|
| `--ground` | `#010e1e` | `#f3f8fd` | أرضية الصفحة |
| `--surface` | `#03192f` | `#e9f0f7` | بانلات، سايدبار |
| `--surface-2` | `#092541` | `#d9e1eb` | سطح مرفوع، صفوف الجداول |
| `--surface-3` | `#173758` | `#c2cdda` | حدود بارزة |
| `--mute` | `#556b82` | `#6c7c8f` | خطوط فاصلة |
| `--field-line` | `#344f6d` | `#6f8298` | حدّ حقول الإدخال |
| `--ink` | `#f6eed8` | `#0e2138` | نص أساسي |
| `--ink-2` | `#c6bda7` | `#3c4d62` | نص ثانوي |
| `--ink-3` | `#a79e8a` | `#516073` | نص ميتا |
| `--flame` | `#f67214` | `#eb5e00` | الفيل البرتقالي / الحرارة |
| `--flame-hi` | `#ffa64d` | `#f58028` | هوفر الفيل |
| `--flame-ink` | `#f67214` | `#ac3800` | برتقالي كنص |
| `--flame-ink-hi` | `#ffa64d` | `#892504` | هوفر النص البرتقالي |
| `--on-flame` | `#010e1e` | `#051427` | حبر فوق البرتقالي |
| `--frost` | `#a4d7ec` | `#3090c2` | علامة −18°م |
| `--frost-ink` | `#a4d7ec` | `#0e6492` | الأزرق كنص |
| `--invert` | `#f8f5ec` | `#03192f` | الباند المعكوس |

في الضلمة، قيم `*-ink` هي **نفس** قيم `*-hi` القديمة بالظبط — عشان كده الوضع الداكن بيترندر زي ما كان حرفيًا.

**السطوح بتتحرّك في اتجاهين مختلفين:** في الضلمة البانل بيطلع فوق الأرضية، في النهار بينزل تحتيها. الاتنين بيدّوا نفس النتيجة — البانل ينفصل عن صفحته.

### إيه اللي منعناه بالقصد

الأرضية الفاتحة **مش كريمي/بيج**. الشريحة الدافئة (L 0.84–0.97، C < 0.06، hue 40–100) هي الرد الانعكاسي على "لايت مود"، وكمان بتتخانق مع البرتقالي. الأرضية هنا قاعدة في عيلة الكحلي بتاعت البراند (hue 250–254) — **أوضة تبريد، مش ورقة**.

### الخامات اللي بتتغيّر مع النور

مش بس الألوان. دي أدوار كمان، وكل واحد فيهم اتغيّر لسبب:

| Token | Dark | Light | السبب |
|---|---|---|---|
| `--grain-blend` / `--grain-opacity` | `overlay` / 0.32 | `multiply` / 0.055 | `overlay` بيرفع الجرين من أرضية غامقة؛ على فاتحة ما بيعملش حاجة خالص |
| `--bloom` | 1 | 0.3 | التوهّج نور بيتسرّب في أوضة ضلمة. على أرضية فاتحة ما فيش حاجة يتسرّب عليها، فبيبان لطخة |
| `--shadow-1..3` | أسود شفّاف | كحلي البراند بألفا واطية | الأسود على أرضية فاتحة بيتقرا **وسخ** مش ظل |
| `--field-bg` | سطح غاطس | أبيض صريح | حقل مظلّل في النهار بيتقرا **disabled** |
| `--chrome` | سطح 45% | سطح كامل | نفس الحجاب على أرضية فاتحة بيقع على بعد شعرة منها والشروم بيبطّل يبان شروم |
| `--picker-filter` | `invert(.85)` | `none` | قلب أيقونة الكالندر بتاعة كروم صح على بانل غامق، وبيمحيها على فاتح |
| `--select-arrow` | شيفرون كريمي | شيفرون كحلي | الـ data URI ما بيقراش custom property، فبيتشحن مرتين |

### الجُزُر الغامقة

ستيدج المنتج في الهيرو **بيفضل غامق في النهار**، وده مش استثناء عشوائي:

الصور المجمّدة والتلج مصوّرة على أسود صافي ومركّبة بـ `mix-blend-screen` — وده اللي بيخلّي خلفيتها تسقط في البانل. و`screen` على بانل فاتح بيطلع **أبيض**، يعني المنتج كان هيختفي حرفيًا. تثبيت البانل على مجموعة التوكنز الغامقة بيخلّي التركيب صحيح — وهي الصورة الصح أصلًا: دي طاقة على فريزر، والطاقة بتوري نورها هي.

نفس المنطق لأي مستند بيتطبع: `orders/print` مثبّت، وكل الأدوار بتترجع لقيم ورق جوّه `@media print` — أبيض/أسود بغضّ النظر عن الوضع اللي على الشاشة.

### المفتاح

- الافتراضي هو **تفضيل النظام**. اختيار المستخدم بيغلب النظام وبيتخزّن في `localStorage`.
- سكربت صغير **متزامن** في الـ `<head>` ([`partials/theme-boot`](resources/views/partials/theme-boot.blade.php)) بيحسم الوضع **قبل أول رسم** — من غيره الصفحة كانت هتومض من الكحلي للنهار كل تنقّلة.
- التبديل نفسه دايرة بتكبر من الزرار (`View Transitions` + `clip-path`). القطع الحاد بين بالتّتين مزعج، وسيبان كل `transition-colors` في الصفحة تشتغل مع بعض أوحش — بتتلطّخ بسرعات مختلفة.
- من غير جافاسكريبت: الوضع الداكن (ديفولت البراند)، والمفتاح بيتخفي لأنه ما ينفعش يشتغل.
- **قيم النهار مكتوبة نسخة واحدة بس.** بلوك `prefers-color-scheme` كان هيخدم بس ناس الجافاسكريبت مقفول عندهم، وهيبقى نسخة تانية من تلاتين رقم لازم تفضل متزامنة.

**قاعدة:** البرتقالي ما يتجاوزش ~8% من مساحة أي شاشة. لو بقى ديكور، يتشال.

### التحقّق

مش مراجعة بالعين. تلات طبقات:

1. **جدول التوكنز** — 62 زوج، الوضعين، فاحص بيركّب الألفا في مساحة **جاما** (زي المتصفّح بالظبط — التركيب في النور الخطي بيدّي أرقام خيالية) وبيرفض أي كروما بره الـ sRGB gamut.
2. **الصفحة المترندرة** — بيمشي على كل عقدة نص، بيطلع الخلفية الفعلية بالتسلّق لفوق، وبيقيس على حجم ووزن الخط الحقيقي. الـ placeholder بيتقاس لوحده لأنه pseudo-element والمشية ما بتشوفهوش.
3. **سلوك المفتاح** — 17 اختبار: بيقلب، بيثبت، بيغلب النظام، بيعدّي التنقّل، بيحترم `reduced-motion`، والستيدج بيفضل غامق.

## Typography

- **العناوين — `Zain`** (800/900). خط عربي هندسي متأثر بالكوفي، عمودياته حادة وضيقة. مختار عشان يشبه ختم على صندوق شحن، مش عشان "يبان مصمم". مرفوض بالقصد: Cairo / Tajawal (المشبعين).
- **النص — `Readex Pro`** (300–600). واسع الفتحات، مصمم أصلًا للوضوح في العربي. تباين حقيقي مع Zain على محور (ضيّق هندسي × واسع إنساني).
- **الأرقام والقياسات — `Martian Mono`** (400–700، لاتيني فقط). مستخدم **حصريًا** لقراءات الأجهزة: `−18°C` · `175°C` · `2.5 kg` · `20–22%` · جداول التغذية. مش للنص العادي — ده يخلّيه costume.

المقياس: `clamp()` سائل، نسبة ≥1.25، وسقف العنوان الرئيسي `5.5rem` (تحت السقف المسموح 6rem). `letter-spacing` أدنى `-0.03em`. عرض السطر مقفول على `68ch`.

## Layout

- شبكة 12 عمود، عرض أقصى `1280px`، هوامش `clamp(1.25rem, 5vw, 5rem)`.
- إيقاع رأسي متغيّر بالقصد: الهيرو والقلي `clamp(6rem, 14vh, 9rem)`، السكاشن التقنية أضيق `clamp(4rem, 9vh, 6rem)`.
- تراكيب غير متماثلة (7/5 و 5/7) بدل نص/نص.
- ممنوع جريد كروت متطابقة. البدائل المستخدمة: لوحة قياسات، جدول مواصفات، تسلسل مرقّم رأسي.

**z-index sematic scale:** `--z-base:0` → `--z-sticky:20` → `--z-fab:40` → `--z-backdrop:50` → `--z-modal:60`.

## Motion

- **لحظة التحميل:** سكرَبر الحرارة في الهيرو بيشتغل مرة واحدة تلقائيًا (0 → 3:30 في ~2.2ث)، فيتحوّل الشكل من مجمّد لذهبي والرقم يعدّ من −18° لـ 175°. القصة كلها في ثانيتين.
- منحنيات خروجية أسّية فقط: `cubic-bezier(0.16, 1, 0.3, 1)` و `cubic-bezier(0.22, 1, 0.36, 1)`. مفيش bounce.
- الظهور بالتمرير (`v-reveal`) بيشتغل **فوق محتوى ظاهر افتراضيًا**: العنصر ظاهر في CSS، والـ JS هو اللي بيخفيه لحظيًا لما IntersectionObserver يشتغل، ومعاه مؤقّت أمان بيرجّع الظهور بعد 1.2ث مهما حصل. يعني لو الـ JS وقع أو الصفحة اتفتحت في تاب مخفي، المحتوى موجود.
- `prefers-reduced-motion: reduce` → كل الحركات تتحوّل لـ crossfade أو انتقال فوري، والسكرَبر بيبتدي من عند النهاية الذهبية.

## Mobile: قشرة أبلكيشن

الموبايل مش نسخة مصغّرة من الديسكتوب — ده بناء تاني بالكامل تحت `lg`:

- **دوك عايم في متناول الإبهام** (`MobileDock`): تبويبات الرئيسية / سنتانا / المواصفات + زرار «اطلب» برتقالي. التبويب النشط بيتحدّد لحظيًا من موضع السكرول (scroll-spy مربوط بـ rAF)، زي أي tab bar أصلي. بيحلّ محل الـ FAB اللي فضل للديسكتوب بس.
- **بوتم شيت حقيقي** (`AppSheet`): مبني على `<dialog>` عشان حبس التركيز و Escape و inert ييجوا من المتصفح مش من كود عندنا. زوّدنا عليه الحاجة الوحيدة اللي المنصّة مابتديهاش: **السحب لتحت للإغلاق** (pointer events، عتبة 96 بكسل). بيستخدمه القائمة وشيت الطلب.
- **هيدر بيختفي وبيرجع**: بينزل برّه الشاشة وانت نازل، وبيرجع أول ما تطلع لفوق — سلوك nav bar أصلي.
- **ديكات سحب** (`.deck`): الرحلة (6 محطات) وقطاعات العملاء بيتحوّلوا لكاروسيل `scroll-snap` full-bleed بنقط تتبّع. فوق `lg` نفس الماركب بيرجع تدفّق عمودي عادي.
- **PWA قابل للتثبيت**: `manifest.webmanifest` بـ `display: standalone` وأيقونات 192/512/maskable مولّدة من الماركة. «إضافة للشاشة الرئيسية» بتفتح الصفحة من غير أي واجهة متصفح.
- **Safe areas** (`env(safe-area-inset-bottom)`) على الدوك والشيت، و`overscroll-behavior: contain`، و`-webkit-tap-highlight-color: transparent`، وحالات ضغط (`active:scale`) بدل الهوفر على أجهزة اللمس.

## Sticky columns

كل عمود قصير جنب عمود طويل بيتثبّت (`lg:sticky lg:top-28 lg:self-start`) بدل ما يسيب فراغ ميّت: صورة سنتانا، شيكارة المواصفات، مقدّمة الرحلة، عنوان الأسئلة، وعرض السعر.

**فخّ اتصاد:** `overflow-hidden` على السكشن بيحوّله لـ scrollport وبيلغي الـ sticky بصمت. الحل `overflow-clip` — بيقصّ من غير ما يعمل حاوية تمرير.

## Components

- `BrandMark` — الطاحونة + الكلمة، SVG مرسوم بالكود (مش صورة) عشان يفضل حاد في أي مقاس.
- `TempScrubber` — `input[type=range]` حقيقي متلبّس، فبيجي معاه دعم كيبورد ولمس و ARIA مجانًا.
- `SpecGauge` — قوس قياس SVG لمواصفات سنتانا، معاه رقم ونص (اللون مش الحامل الوحيد للمعنى).
- `RevealOnScroll` — directive عام `v-reveal` بـ stagger اختياري.
- `QuoteForm` — تحقّق حقيقي + رسايل خطأ مرتبطة `aria-describedby`، والإرسال بيركّب رسالة واتساب جاهزة (مفيش باك إند).

## لوحة التحكم — نفس العالم، قواعد تانية

اللوحة بتورّث كحلي زيلاند وبرتقاليه وخطوطه، لكن الزائر اتغيّر: ده حد **وسط شغل**،
مش حد بيتقنع. فالقواعد بتتغيّر معاه، والملف اللي بيحكمها `resources/css/admin.css`.

| القاعدة | اللاندينج | اللوحة | ليه |
|---|---|---|---|
| مقياس الخط | `clamp()` سائل | rem ثابت، نسبة ~1.15 | عنوان بيصغّر جوه بانل بيبان مكسور مش متجاوب |
| العناوين | `Zain` | `Readex Pro` (Zain في شاشة الدخول بس) | خط عرض في تسمية زرار = تنكّر |
| الأرقام | `Martian Mono` للقراءات | `Martian Mono` **لكل رقم** | اللوحة كلها أرقام؛ الأعمدة بتتراص بـ `tnum` |
| اللون | Drenched | Restrained + مفردات حالة | البرتقالي للفعل بس، مش للزينة |
| الحركة | 400–900ms سردية | 150–250ms وظيفية | المستخدم في نص مهمة، مش بيتفرّج |

**مفردات الحالة** (`--color-good` / `--color-warn` / `--color-bad` / `--color-idle`):
بتظهر كعلامات صغيرة بس، و**دايمًا مع كلمة**. «مدفوع جزئي» مكتوبة جنب اللون،
و«متأخّر» مكتوبة جنب عدد الأيام — اللون مش الحامل الوحيد للمعنى أبدًا.

**تراكيب متعمدة بدل جريد الكروت المتطابقة:**
- **لوحة اليوم:** رقم واحد ضخم (فلوس عند العملاء) + رسم 12 شهر جنبه، وتحتهم
  «شريط أجهزة» واحد مقسوم بخطوط شعرية بدل 4 كروت متساوية.
- **العميل:** الرصيد أكبر حاجة في الصفحة، والتواصل والشروط جنبه، ورسم 6 شهور
  بيوضّح لو العميل سكت.
- **الأوردر:** الحساب مثبّت (`sticky`) جنب سطور الأصناف — الرقم اللي بتقرّر عليه
  مايتسحبش برّه الشاشة.
- **المقال:** مساعد السيو مثبّت على الجنب، فيه دايرة الدرجة ومعاينة نتيجة جوجل
  و15 فحص، وكلهم بيتحدّثوا وانت بتكتب.

**الرسوم البيانية مرسومة بعناصر عادية** — مفيش مكتبة رسم. وكل رسم معاه نفس
البيانات كجدول `sr-only`.

> **فخّ اتصاد:** الأعمدة بارتفاع نسبي محتاجة أب بارتفاع **محدّد**. مع
> `items-end` كل `<li>` بياخد ارتفاع محتواه، فالنسبة بتتحسب على `auto`
> وكل الأعمدة بتطلع صفر. الحل `items-stretch`، والعمود نفسه لازم يكون `block`
> عشان `height` تشتغل على عنصر inline.

**الطباعة:** كشف الحساب وإذن التسليم مستندات ورق فعلًا — أسود على أبيض، من غير
واجهة، ومن غير أي اعتماد على اللون. الأدوار كلها بتترجع لقيم ورق في
`@media print` جوّه `theme.css`، فمستند مطبوع بيطلع **زيّه بالظبط** سواء اللي طبعه
شغّال على الوضع الفاتح أو الغامق — والبديل كان مطاردة كل كمبوننت بـ `!important`
ولسه بننسى النص الخافت.

**النور:** اللوحة بتاخد نفس التوكنز بتاعت الموقع من `theme.css` ونفس المفتاح —
موظف بيقفل الموقع ويفتح اللوحة بيلاقيها بنفس النور اللي اختاره. المفتاح في
التوب بار عشان يبقى موجود في كل شاشة.

## Imagery

كل الصور محمّلة محليًا في `public/img/` (مفيش hotlink، ومفيش اعتماد على طرف تالت وقت العرض).

| ملف | إيه اللي بيثبته |
|---|---|
| `fries-dark.jpg` | البطاطس **مجمّدة** على أسود — حالة المنتج الحقيقية وقت التسليم |
| `fries-heap.jpg` | ماكرو للقرمشة بعد القلي — الوعد |
| `fry-basket-dark.jpg` | سلة قلي في ضوء خافت — لحظة الحرارة |
| `fryer-lift.jpg` | مطبخ خدمة حقيقي، سلة بتترفع — سياق B2B |
| `kitchen-chefs.jpg` | مطبخ احترافي شغّال |
| `potato-harvest.jpg` | درنات في إيد — الحقل والصنف |
| `frost-dark.jpg` | ماكرو تلج أزرق — سلسلة التبريد |
| `pack.jpg` | آرت وورك العبوة (وش وضهر) — مصدر أرقام المواصفات |

**صور البراند** (مقصوصة ومضغوطة من ملفات الاستوديو في `img/`):

| ملف | إيه اللي بيثبته | مكانه |
|---|---|---|
| `brand-pack.jpg` | العبوة الحقيقية في مشهد مطبخ دافي | الباند الختامي |
| `brand-kitchen.jpg` | العبوة + سلة مقلية + شيف في نفس الكادر | خلفية الباند الختامي (مغمّقة ومبلوّرة) |
| `brand-bowl.jpg` | البطاطس متقدّمة في طبق خشب | كافيهات وفنادق |
| `brand-board.jpg` | العبوة واقفة جنب لوح تقديم | موزّعين وجملة |
| `brand-pour.jpg` | البطاطس بتتفرغ من الشيكارة على لوح | احتياطي |

قاعدة: كل صورة بتثبت حاجة. مفيش صورة ديكور.

**ملاحظة على الصور دي:** إنتاج استوديو/رندر، ونصوص ضهر العبوة فيها غير مقروءة. علشان كده الكروب دايمًا على وش العبوة، وسكشن المواصفات فضل شغّال على `pack.jpg` (الآرت وورك الأصلي) لأن أرقامه هي المصدر.
